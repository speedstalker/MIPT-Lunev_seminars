#include <stdio.h>
#include <string.h>

int main (int argc, char** argv)
{
int i = 0, numb = 0, len = 0;
char symb = 0;
char* str = NULL;

if ( (argc < 1) || (argc > 3) )
	return 0;
	
str = argv[1];

len = strlen (str);

for (i = 0; i < len; i++)
	{
	symb = str [i];
	if ( (0 <= symb) && (symb <= 9) )
		numb = numb * 10 + symb;
	symb = 0;
	
	}
	
for (i = 0; i < numb; i++)
	{
	printf ("%d", i);
	
	if (i < (numb - 1) )
		printf ("\n");
	}

return 0;
}
